{
  "schema_version": 1,
  "local_migration_head": "0070",
  "staging": {
    "migration_head": "0070",
    "deploy_enabled": true,
    "release_enabled": true,
    "hold_reason": "WP-280: retry worker/runtime config and required pg_net transport (0066-0070) are authorized only for staging activation and GitHub beta device QA."
  },
  "production": {
    "migration_head": "0070",
    "deploy_enabled": true,
    "release_enabled": false,
    "hold_reason": "Owner-directed 0066-0070 production migration promotion is in progress; release remains exact-GO gated."
  }
}
