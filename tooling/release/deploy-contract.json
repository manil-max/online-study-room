{
  "schema_version": 1,
  "local_migration_head": "0067",
  "staging": {
    "migration_head": "0067",
    "deploy_enabled": true,
    "release_enabled": true,
    "hold_reason": "WP-268: push delivery/runtime config (0066-0067) is authorized only for staging activation and beta-v4302 device QA."
  },
  "production": {
    "migration_head": "0065",
    "deploy_enabled": true,
    "release_enabled": true,
    "hold_reason": "0065 IS applied to production, but manually via docs/recovery/sql/apply_0065_production_manual.sql (verified: marathon_total tier1 = 1500 XP, 42 ledger rows repriced) because production has NO supabase_migrations history (PRODUCTION-BASELINE.md 63). Chain repair remains WP-232."
  }
}
