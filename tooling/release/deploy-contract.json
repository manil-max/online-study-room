{
  "schema_version": 1,
  "local_migration_head": "0089",
  "staging": {
    "migration_head": "0089",
    "deploy_enabled": true,
    "release_enabled": true,
    "hold_reason": "WP-373 adds 0089: it schedules the lease sweeper that 0082 defined but never wired up. Paired with the client's new 60s heartbeat, a live run keeps its lease while it runs and a crashed device's run is closed within a minute instead of staying 'running' forever. Schema is unchanged - the migration only creates one pg_cron job. Staging applies first so production repeats a proven step."
  },
  "production": {
    "migration_head": "0089",
    "deploy_enabled": false,
    "release_enabled": true,
    "backup_requirement": "waived",
    "backup_waiver": {
      "decided_by": "owner",
      "decided_at_utc": "2026-07-27T00:00:00Z",
      "reason": "Free plan project has no daily backup and no PITR; owner accepted production apply with no rollback path.",
      "rollback_path": "none"
    },
    "hold_reason": "RELEASE WINDOW OPEN for v54 (owner GO on 2026-07-28, after confirming nothing but the parked tablet layout is left in the md files). deploy_enabled stays false: 0089 is already applied on all three environments (production run 30307084863, staging run 30303743005, post-check head 0089 on both) and any further production migration needs its own explicit GO. This window is reclosed immediately after the tag is cut. v54 carries the client half of WP-373, which is what actually fixes multi-device timer sync - both devices need this build; 0089 was only the crash-safety net. It also ships WP-374 (feedback conversation), WP-375 (intro tour), WP-376 (achievements IA), WP-377 (campfire day/night plus the owner-selected composition) and WP-378 (announcement signal). The backup waiver stays recorded because the project still has no PITR or daily backup."
  }
}
