{
  "schema_version": 1,
  "local_migration_head": "0123",
  "staging": {
    "migration_head": "0122",
    "deploy_enabled": false,
    "release_enabled": false,
    "hold_reason": "RE-LOCKED after the 2026-08-08 v60 chain: migration 0122 (name length limits) was applied to staging and the post-check reported head 0122 (run 31256365815). Both heads now read 0122 in all four pin locations. release_enabled stays false ON PURPOSE - the stable release passes with a single-use PRODUCTION RELEASE GO confirmation string instead of an open flag. No further apply or release is authorized; a new scoped owner authorization is required."
  },
  "production": {
    "migration_head": "0122",
    "deploy_enabled": false,
    "release_enabled": false,
    "backup_requirement": "waived",
    "backup_waiver": {
      "decided_by": "owner",
      "decided_at_utc": "2026-07-27T00:00:00Z",
      "reason": "Free plan project has no daily backup and no PITR; owner accepted production apply with no rollback path.",
      "rollback_path": "none"
    },
    "hold_reason": "RE-LOCKED after the 2026-08-08 v60 chain: migration 0122 (name length limits) was applied to production and the post-check reported head 0122 (run 31256510889). Both heads now read 0122 in all four pin locations. release_enabled stays false ON PURPOSE - the stable release passes with a single-use PRODUCTION RELEASE GO confirmation string instead of an open flag. No further apply or release is authorized; a new scoped owner authorization is required. The free-plan no-backup/no-PITR/no-rollback waiver stays on record (production.backup_waiver, owner 2026-07-27)."
  }
}
