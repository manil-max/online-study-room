{
  "schema_version": 1,
  "local_migration_head": "0116",
  "staging": {
    "migration_head": "0114",
    "deploy_enabled": false,
    "release_enabled": false,
    "hold_reason": "CONSUMED AND RE-LOCKED (2026-07-31): the scoped one-shot staging apply GO was used by Database Gates run 30660596728 and staging is now genuinely at migration head 0114 (0101-0114 pushed, post-check reports local|remote|file = 0114). The authorization is spent. Any further staging apply or beta release requires a new scoped contract update with its own replay, tests, target SHA/head and QA evidence. release_enabled stays false: beta release is a separate WP-466 decision. Production untouched and on HOLD."
  },
  "production": {
    "migration_head": "0100",
    "deploy_enabled": false,
    "release_enabled": false,
    "backup_requirement": "waived",
    "backup_waiver": {
      "decided_by": "owner",
      "decided_at_utc": "2026-07-27T00:00:00Z",
      "reason": "Free plan project has no daily backup and no PITR; owner accepted production apply with no rollback path.",
      "rollback_path": "none"
    },
    "hold_reason": "HOLD AFTER V56 (2026-07-30): production is already at migration head 0100 and the v56 stable release is complete. The explicit owner GO from 2026-07-28 was scoped only to that production apply and v56 tag and is now consumed. Any future production migration, Edge deployment or stable release requires a new concrete owner GO plus the then-current staging, device QA, soak, backup/waiver, dry-run and exact SHA/head evidence."
  }
}
