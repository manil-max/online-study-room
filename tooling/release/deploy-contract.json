{
  "schema_version": 1,
  "local_migration_head": "0072",
  "staging": {
    "migration_head": "0070",
    "deploy_enabled": true,
    "release_enabled": true,
    "hold_reason": "WP-280: retry worker/runtime config and required pg_net transport (0066-0070) are authorized only for staging activation and GitHub beta device QA. PENDING: 0071 (group member limit 8) and 0072 (private feedback attachment bucket/RLS repair) are written but NOT applied to any environment yet; staging apply must run before they can ship."
  },
  "production": {
    "migration_head": "0070",
    "deploy_enabled": false,
    "release_enabled": false,
    "hold_reason": "WP-293: owner-directed 0066-0070 production promotion is COMPLETE (Database Gates + Production Push Activation runs succeeded 2026-07-23); the one-time deploy gate is re-locked. Effective production schema is 0070, but CLI migration history remains unreconciled (docs/recovery/PRODUCTION-BASELINE.md). Any next production apply/release requires a fresh, explicit owner GO with backup + dry-run evidence."
  }
}
