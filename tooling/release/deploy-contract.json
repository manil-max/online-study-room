{
  "schema_version": 1,
  "local_migration_head": "0085",
  "staging": {
    "migration_head": "0085",
    "deploy_enabled": true,
    "release_enabled": true,
    "hold_reason": "WP-348 staging acceptance: 0073-0085, including server-authoritative primary-group cooldown, await sequential staging dry-run/apply, post-check and beta device evidence."
  },
  "production": {
    "migration_head": "0085",
    "deploy_enabled": true,
    "release_enabled": false,
    "backup_requirement": "waived",
    "backup_waiver": {
      "decided_by": "owner",
      "decided_at_utc": "2026-07-27T00:00:00Z",
      "reason": "Free plan project has no daily backup and no PITR; owner accepted production apply with no rollback path.",
      "rollback_path": "none"
    },
    "hold_reason": "WP-351: production 0085 apply is unblocked - staging 0085 apply/post-check and the protected production dry-run passed, and backup/PITR evidence is now derived in CI from the Supabase backups API (fail-closed). Public stable release stays HOLD until the production post-check reports head 0085."
  }
}
