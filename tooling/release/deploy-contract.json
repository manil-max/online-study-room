{
  "schema_version": 1,
  "local_migration_head": "0088",
  "staging": {
    "migration_head": "0087",
    "deploy_enabled": true,
    "release_enabled": true,
    "hold_reason": "Faz F5 (WP-367/WP-368) adds 0086 (presence heartbeat renews the projection lease) and 0087 (global timer v2 server switch on). Staging applies first so production repeats a proven step."
  },
  "production": {
    "migration_head": "0087",
    "deploy_enabled": false,
    "release_enabled": false,
    "backup_requirement": "waived",
    "backup_waiver": {
      "decided_by": "owner",
      "decided_at_utc": "2026-07-27T00:00:00Z",
      "reason": "Free plan project has no daily backup and no PITR; owner accepted production apply with no rollback path.",
      "rollback_path": "none"
    },
    "hold_reason": "Faz F5 closed 2026-07-27: 0086 and 0087 applied under the owner's explicit order, production post-check reported head 0087 (run 30288908244), and v52 stable shipped with all jobs green (run 30289549858). Deploy and release are re-locked to the default HOLD; the next production migration or stable release needs its own explicit owner GO. The backup waiver stays recorded because the project still has no PITR or daily backup - it is not a standing approval to apply. Known open limit carried into the next round: nothing renews the live_study_runs 150s lease and no cron drives the sweeper, so a crashed device leaves its run 'running' until the next start adopts it."
  }
}
