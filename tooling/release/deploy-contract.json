{
  "schema_version": 1,
  "local_migration_head": "0085",
  "staging": {
    "migration_head": "0085",
    "deploy_enabled": true,
    "release_enabled": true,
    "hold_reason": "Staging is at 0085 (WP-348 apply and post-check passed). Beta device acceptance for the V3 chain is still open; rollout flags stay off."
  },
  "production": {
    "migration_head": "0085",
    "deploy_enabled": false,
    "release_enabled": false,
    "backup_requirement": "waived",
    "backup_waiver": {
      "decided_by": "owner",
      "decided_at_utc": "2026-07-27T00:00:00Z",
      "reason": "Free plan project has no daily backup and no PITR; owner accepted production apply with no rollback path.",
      "rollback_path": "none"
    },
    "hold_reason": "WP-351 closed 2026-07-27: baseline repaired to 0070, 0071-0085 applied, post-check reported head 0085 and v49 stable shipped. Deploy is re-locked to the default HOLD; the next production migration needs its own explicit owner GO. The backup waiver stays recorded because the project has no PITR or daily backup - it is not a standing approval to apply."
  }
}
