{
  "schema_version": 1,
  "local_migration_head": "0132",
  "staging": {
    "migration_head": "0132",
    "deploy_enabled": true,
    "release_enabled": false,
    "hold_reason": "OPEN for exactly ONE staging apply of migrations 0131 and 0132. Scope: 0131 + 0132 only. WHAT 0131 DOES (lane J): goal_progress_events has NO foreign key at all (0112:27-41) and its name never appears in 0113, 0114, 0124 or purge-accounts/index.ts - so a deleted account's completion events stayed in the database forever, and so did a purged group's. 🔴 The obvious fix was measured and REJECTED: scope_id is polymorphic (auth.uid() for personal, groups.id for group, 0120:117), so a single FK to auth.users would violate every group row and the migration would fail AT APPLY TIME. Instead: two AFTER DELETE triggers on public.profiles and public.groups plus a one-shot cleanup. The triggers only DELETE, they never write, so the 23503 class that broke account deletion in 0124 cannot occur - and 057 asserts lives_ok(delete from auth.users) anyway rather than assuming. WHAT 0132 DOES (lane J): the group half of the goal-day total counted only TODAY's members (gm.left_at is null) in three separate copies (0120:88, 0129:114, 0129:299). A member who contributed and then left had their seconds erased retroactively, so an unrelated edit on that day could silently delete a group streak the group had genuinely earned. The rule was already written elsewhere in this schema - 0121:186-191 builds the group ACHIEVEMENT projection with (left_at is null or start_time < left_at) - the streak path simply never copied it. 0132 collapses all copies into one definition and applies the membership window. The change is one-directional: it only ADDS back seconds that were wrongly excluded, so no group day total can shrink and no new retraction can be produced. 🔴 0132 ALSO REPAIRS A SIDE EFFECT I CAUSED THIS MORNING. 0129's apply block calls _repair_orphan_goal_completions (0129:349) and that function's group branch used the today's-members predicate, deleting a group day whenever every contributor had since left. Blast radius was MEASURED, not guessed: the delete criterion was TOTAL ZERO seconds, not below-goal, so only groups where every contributor left are affected. The repair mirrors the delete exactly - three conditions together: no event exists now, the OLD predicate yields zero, and the CORRECTED predicate meets the goal. 🔴 HONEST LIMIT, written into the migration: the third condition uses the group's CURRENT daily_goal_minutes because the historical goal is stored nowhere. A group that LOWERED its goal since could get back a day it did not earn. The alternative - restoring nothing - would leave genuinely earned days deleted. The smaller of two wrongs was chosen deliberately. backfill_goal_completions() was NOT called: it re-judges all history by today's goal and would invent unearned days for anyone who lowered theirs. EVIDENCE AND ITS LIMIT: pgTAP did NOT run on the dev host (Docker engine will not start). supabase/tests/057, 058 and 059 were WRITTEN but never executed here - recorded as OLCEMEDIM, not green. The CI local replay inside this run is their FIRST execution, exactly as it was for 055/056 earlier today, where that same honesty paid off. BOTH ENVIRONMENTS IN ONE SESSION: same standing instruction the owner gave for 0129+0130 on 2026-08-10 - apply staging and production together, do not wait between them. Staging still goes FIRST and its post-check is read before production is dispatched. Once the post-check reports head 0132 this flag is re-locked to false in a separate commit. release_enabled stays false ON PURPOSE. PREVIOUS SCOPE (spent, SUCCEEDED): RE-LOCKED on 2026-08-10 after the 0129 + 0130 apply SUCCEEDED in BOTH environments. EVIDENCE: staging Database Gates run 31380186208 (post-check reported 0129 and 0130 in all three columns on rskiuyjabyzelqododpa, purge health configured / 0 queued / 0 stuck, no SQLSTATE); production run 31380542690 (pre-check showed the two rows missing on the remote side, post-check reported 0129 and 0130 in all three columns on jiphfrpzvkpzubbkhrwb). 🔴 The most useful line of this round: pgTAP could not run on the dev host (Docker engine will not start), so supabase/tests/055 and 056 were delivered as OLCEMEDIM - the CI local replay inside these two runs was the FIRST execution of both files, and both passed on real Postgres. The gate that could not run locally is exactly the gate that mattered. Scope spent; both flags close. migration_head stays 0130 because that is the real state of both environments. PREVIOUS SCOPE (spent, SUCCEEDED): OPEN for exactly ONE staging apply of migrations 0129 and 0130. Scope: 0129 + 0130 only. WHAT 0129 DOES (WP-654): deleting a study session did not give the daily streak back. Root cause read from SQL, not from a card: of the four projection triggers on study_sessions, three listen to delete (0050:202, 0063:214, 0063:504) and one does not - 0120:249 is 'after insert or update'. Its body only calls _record_goal_completion (0120:219,239), so the path was MONOTONIC APPEND. 0126 does listen to delete but the table the streak is derived from (goal_progress_events, 0112:204) is never named in 0126 or 0128. 0129 adds the symmetric retraction for both DELETE and UPDATE (shortening a session hit the same gap), skips accounts being deleted with the _account_still_exists predicate 0124 introduced, and carries a DELIBERATELY NARROW backfill: only events with zero remaining seconds behind them are dropped, so a user who later RAISED their daily goal does not lose past streak days they genuinely earned. WHAT 0130 DOES (WP-656): nine FAQ sentences described the product incorrectly. The heaviest was 'signing in again cancels your deletion request' - false, and dangerous: cancelAccountDeletion is only reachable from account_settings_screen.dart:443,596 and has ZERO call sites in the auth flow, so a user trusting that sentence LOSES THE ACCOUNT. Second heaviest: the crown answer cited crownTierFor (gamification.dart:124-128) whose output field is read by no screen at all - the FAQ was citing DEAD CODE as its evidence. 0130 corrects nine entries and adds six missing ones (pricing/ads, XP reversal on delete, the countdown card, the Windows build, no-coercion policy, starting from widget/notification). It carries its own apply guard: a do-block that raises faq_correction_target_missing if any of the 18 target rows is absent, so a silent zero-row UPDATE cannot pass as success. EVIDENCE AND ITS LIMIT - STATED PLAINLY: pgTAP did NOT run locally. Docker Desktop's engine does not start on this host (measured: docker info fails on the npipe endpoint; psql and supabase CLI are absent), so supabase/tests/055 (22 assertions) and 056 were WRITTEN but never executed. This is recorded as OLCEMEDIM, not as green. What WAS measured: the trigger inventory above was read from the migration sources; the FAQ corrections were verified against app code file:line; a Python harness rebuilt the FAQ corpus from 0091+0118+0123, applied 0130 and ran every assertion of 056 over it (66 rows -> 78, TR 39 / EN 39, 21 passed, 0 red), then a sabotage that restored one falsified sentence turned it red. That harness does NOT validate SQL syntax or the do-block guard; the CI database-gates local replay is the first place those run. BOTH ENVIRONMENTS IN ONE SESSION: the owner asked on 2026-08-10 to apply staging and production together rather than wait between them, and was told first that skipping the staging soak saves about three minutes and that 0126 shipped a regression the previous night for exactly that class of reason (repaired by 0128). The owner reaffirmed. Staging is still applied FIRST and its post-check read before production is dispatched - the sequence is kept, only the waiting is dropped. Once the post-check reports head 0130 this flag is re-locked to false in a separate commit. release_enabled stays false ON PURPOSE. PREVIOUS SCOPE (spent, SUCCEEDED): RE-LOCKED on 2026-08-10 after the 0128 staging apply SUCCEEDED (run 31335734360, post-check 0128 on both sides). Staging reported '0 kilit satiri geri kondu' - no damaged rows there, which is expected since the regression hit accounts that had lost tiers, and staging has different data. Scope spent. PREVIOUS SCOPE (spent, SUCCEEDED): OPEN for exactly ONE staging apply of migration 0128 (WP-635: repair). Scope: 0128 only. 🔴 THIS FIXES A REGRESSION I SHIPPED. 0126's reconciliation matched the single user_achievements row by 'tier = <lost tier>' and DELETED it. That table holds one row per achievement (0022:26) whose tier column is the HIGHEST reached tier, so an account with steel_will at tier 6 lost the whole row when tiers 5 and 6 became unearned - and with it the display of tiers 1-4 that were still legitimately earned. Measured on the owner's own account: xp_ledger held steel_will t1..t4, day_hero t1..t5, marathon_total t1 while the screen showed zero tiers. The data survived; the row the UI reads did not. WHAT 0128 CHANGES: the unlock row is LOWERED to the highest still-earned tier instead of deleted, and is removed only when no tier remains. It also pulls achievement_metric_progress down for the three session-derived metrics - that projection is cumulative-greatest by design (0050:296) so it never decreased, which is why the screen still read 'Best 480' after an 8-hour session was deleted. Out-of-scope achievements and metrics are untouched. REPAIR: deleted unlock rows are rebuilt by DERIVING the highest tier from the surviving ledger - not guesswork. The migration then verifies itself: no achievement that still has a ledger tier may be missing from the unlock table, or it raises and rolls back. EVIDENCE: Database Gates green on 48e1c53; supabase/tests/054 passes 6 assertions and covers the exact gap 052 could not see - 052's 'preserved' assertion read the LEDGER, not the row the screen reads. Once the post-check reports head 0128 this flag is re-locked in a separate commit. PREVIOUS SCOPE (spent, SUCCEEDED): RE-LOCKED on 2026-08-09 after the 0127 staging apply SUCCEEDED. EVIDENCE: Database Gates run 31333835770, post-check reported 0127 on both sides with no SQLSTATE. 🔴 The run also produced the single most useful line of the night: the LOCAL replay logged 'WP-634 backfill: 0 kullanici uzlastirildi' while the REMOTE staging apply logged '1 kullanici uzlastirildi'. The local gate measured nothing because a fresh database has no profiles - exactly the blind spot that let 0124 reach production earlier the same night - while the real environment did real work. Scope spent. PREVIOUS SCOPE (spent, SUCCEEDED): OPEN for exactly ONE staging apply of migration 0127 (WP-634 backfill). Scope: 0127 only. The previous scope (0126) is SPENT and SUCCEEDED - Database Gates run 31333036931, post-check reported 0126 on both sides with no SQLSTATE. This entry re-locks that scope by superseding it and opens a new one rather than silently reusing the old flag. WHAT 0127 DOES: 0126 installs the trigger, but a trigger only fires at delete time - a user who deleted their session BEFORE the trigger existed has nothing to fire. That is how this incident started. 0127 runs the reconciliation once over existing users so history is cleaned too. TARGETING: no user is named. The repository is public, so no one's email goes into a migration; and a targeted migration would MISS anyone else in the same state. The rule looks at the condition, not the person. SELF-VERIFYING: after reconciling, every profile's balance must equal its own ledger sum; otherwise the migration raises and the transaction rolls back. supabase/tests/053 proves that check is not an empty sentence - on a fresh replay gamification_profiles is EMPTY, so the loop never turns and the migration would pass while measuring nothing (the exact gap that let 0124 reach production the same night). 053 builds the state by hand, runs the reconciliation, then deliberately corrupts a balance and asserts the drift query sees it. EVIDENCE: Database Gates green on aa20aaf - clean local replay of 0127 plus tests 052 (12 assertions) and 053. Once the post-check reports head 0127 this flag is re-locked to false in a separate commit. PREVIOUS SCOPE (spent, SUCCEEDED): OPEN for exactly ONE staging apply of migration 0126 (WP-634: deleting a study session now reverses the XP, achievement tiers and crown it produced). Scope: 0126 only. WHY: XP is not a function of the session list, it is a residue of session events - the append-only xp_ledger was never told when a session was deleted. A user's timer ran all night, wrote an ~11 hour phantom session, and deleting it left the XP, tiers and crown in place. Diagnosis: docs/analiz/WP-595-sayac-xp-teshis.md. HOW: it does not subtract. It RECOMPUTES from the user's real sessions and drops only what is no longer earned, then re-derives the balance as the ledger sum. Scope is deliberately narrow - hour XP, marathon_total, steel_will and day_hero. Achievements that other paths award (alpha_wolf, campfire_hours, locomotive, secret_break_enemy) and cumulative-greatest metrics (fire_streak, weekend_goal_days, perfect_month) are untouched; treating their progress as zero would have destroyed genuinely earned badges. ACCOUNT-DELETION HAZARD: an AFTER DELETE trigger on study_sessions is the exact class that made account deletion impossible in this repo (0124/WP-549). The trigger is silenced by _account_still_exists, the same predicate 0124 introduced, and it is statement-level with an old-table transition so deleting many sessions does not recompute per row. EVIDENCE: Database Gates green on 8c7eb16 - supabase/tests/052 passes 12 assertions on real Postgres, including the reverse ones that matter: an out-of-scope achievement and another user's ledger are preserved, the still-earned tier of the SAME achievement survives while the unearned tier is dropped, and 'delete from auth.users' still succeeds with the new trigger installed. An earlier run failed 2 of 11 and both were wrong expectations in the test, not defects: the fixture grants an extra hour, and day_hero tier 1 was still legitimately earned. AUTHORIZATION: the owner asked for this explicitly on 2026-08-09 and asked that it be automatic from now on. Once the post-check reports head 0126 this flag is re-locked to false in a separate commit, together with the guard.tests.ps1 head assertions. release_enabled stays false ON PURPOSE. PREVIOUS SCOPE (spent, SUCCEEDED): RE-LOCKED on 2026-08-09 after the 0125 staging apply SUCCEEDED. EVIDENCE: Database Gates run 31325827247, operation staging-apply on commit 85f4ce60d1d492f280ff6591c12af512928af8c5; the migration list post-check reports 0125 in all three columns on rskiuyjabyzelqododpa with no SQLSTATE. migration_head stays 0125 because that is the real state of staging; only the gate closes. The scope was one apply and it is spent. PREVIOUS SCOPE (spent, SUCCEEDED): OPEN for exactly ONE staging apply of migration 0125 (WP-630: monthly report consent default and the cron's target month). Scope: 0125 only. WHAT IT CHANGES: (a) profiles.monthly_report_opt_in default true -> false plus a one-shot backfill of rows that are not already false; (b) reschedules the monthly-report-collector cron with the SAME expression, dropping only the request body so collect-reports computes the previous month itself. No schema, policy, grant or function changes beyond that cron job. BLAST RADIUS: one boolean column on public.profiles and one pg_cron job. The UPDATE touches only monthly_report_opt_in; both triggers on profiles are column-scoped (profiles_public_name_filter on display_name, profiles_title_earned_guard on title_achievement_id) so neither fires - checked, not assumed, per the 0124 lesson. There is no immutability guard on profiles, so the 42501 class that broke 0124 cannot occur here. USER-VISIBLE EFFECT: none. No monthly report email has ever been sent (send-report has no caller and is deployed nowhere, measured in WP-626), so no row carrying true represents a deliberate choice, and the UI has said 'not being sent yet' since WP-626. EVIDENCE: Database Gates run 31325352521 and the follow-up green run on 589c7f8 - clean local replay on real Postgres with pgTAP, including the new supabase/tests/051 which builds a real row by hand and asserts the backfill statement actually closes it (a fresh replay leaves profiles empty, so without that the statement would be unmeasured - the exact gap that let 0124 reach production). The same cron block already ran successfully in this environment as part of 0035. WHY NOW: adding 0125 re-locked the release line (release-preflight.ps1 requires local head == contract head), so no Windows or Play release can be cut until this is applied. AUTHORIZATION: the owner delegated migration applies in-session on 2026-08-09 conditional on certainty. Once the post-check reports head 0125 this flag is re-locked to false in a separate commit, together with the guard.tests.ps1 head assertions. release_enabled stays false ON PURPOSE. PREVIOUS SCOPE (spent): RE-LOCKED on 2026-08-09 (WP-577) after the WP-549 apply completed. EVIDENCE: Database Gates run 31277610025 succeeded and the post-check reported head 0124 on both sides, with the purge health row reading configured / 0 queued / 0 stuck. The previous OPEN scope was spent by that single apply; the contract's own written commitment was to re-lock in a separate commit and the flag had simply been left open. migration_head stays 0124 because that is the real state of staging - only the gate closes. No further apply or release is authorized without a new scoped owner authorization. PREVIOUS SCOPE (spent): OPEN for exactly ONE staging apply of migration 0124 (WP-549: account deletion was blocked by four indirect restrict FK chains, five write-back triggers and two immutability guards). Scope: 0124 only. It changes FK actions on live_study_segments, study_sessions, global_timer_commands, moderation_appeals and ugc_reports, adds two snapshot/hash columns, and re-creates nine trigger functions behind an `account still exists` gate. EVIDENCE: CI local replay (real Postgres + pgTAP) is GREEN on commit 5d64464 - run 31277161339, job 'Local replay, RLS and invariant gate' succeeded, including the new supabase/tests/050 (26 assertions) and the previously failing 001 and 011 contracts. Two earlier attempts were RED and both were real defects, not flakes: run 31276032801 (achievement write-back for an already-deleted user) and run 31276906884 (the fix had reverted WP-336 session-to-group attribution). WHY: Play requires account deletion and docs/legal/ACCOUNT-DELETION.* promises it unconditionally; with these chains open that promise was factually false for anyone who had ever run the timer. Owner granted permission in-session on 2026-08-08 after being shown this exact scope. Once the post-check reports head 0124 this flag is re-locked to false in a separate commit, together with the guard.tests.ps1 head assertions. release_enabled stays false ON PURPOSE - this migration needs no new app release. PREVIOUS SCOPE (spent): RE-LOCKED after the 2026-08-08 WP-522 apply: migration 0123 (FAQ content expansion) was applied to staging and the post-check reported head 0123 (run 31258097990). ORIGINAL OPEN SCOPE was exactly ONE staging apply of migration 0123 (WP-522 FAQ content expansion, 40 rows, TR+EN). Content-only: no schema, function, policy or grant changes, so the blast radius is the faq_entries table alone. Authorized by the owner in-session on 2026-08-08 (durable release authorization, reaffirmed after the v60 device acceptance where the FAQ was the single remaining complaint). Once the post-check reports head 0123 this flag is re-locked to false in a separate commit, together with the guard.tests.ps1 head assertions. release_enabled stays false ON PURPOSE - no new app release is needed, FAQ rows are read from the server and appear in the installed v60. That scope is now spent; no further apply or release is authorized without a new scoped owner authorization."
  },
  "production": {
    "migration_head": "0132",
    "deploy_enabled": true,
    "release_enabled": false,
    "backup_requirement": "waived",
    "backup_waiver": {
      "decided_by": "owner",
      "decided_at_utc": "2026-07-27T00:00:00Z",
      "reason": "Free plan project has no daily backup and no PITR; owner accepted production apply with no rollback path.",
      "rollback_path": "none"
    },
    "hold_reason": "OPEN for exactly ONE production apply of migrations 0131 and 0132. Scope: 0131 + 0132 only. WHAT 0131 DOES (lane J): goal_progress_events has NO foreign key at all (0112:27-41) and its name never appears in 0113, 0114, 0124 or purge-accounts/index.ts - so a deleted account's completion events stayed in the database forever, and so did a purged group's. 🔴 The obvious fix was measured and REJECTED: scope_id is polymorphic (auth.uid() for personal, groups.id for group, 0120:117), so a single FK to auth.users would violate every group row and the migration would fail AT APPLY TIME. Instead: two AFTER DELETE triggers on public.profiles and public.groups plus a one-shot cleanup. The triggers only DELETE, they never write, so the 23503 class that broke account deletion in 0124 cannot occur - and 057 asserts lives_ok(delete from auth.users) anyway rather than assuming. WHAT 0132 DOES (lane J): the group half of the goal-day total counted only TODAY's members (gm.left_at is null) in three separate copies (0120:88, 0129:114, 0129:299). A member who contributed and then left had their seconds erased retroactively, so an unrelated edit on that day could silently delete a group streak the group had genuinely earned. The rule was already written elsewhere in this schema - 0121:186-191 builds the group ACHIEVEMENT projection with (left_at is null or start_time < left_at) - the streak path simply never copied it. 0132 collapses all copies into one definition and applies the membership window. The change is one-directional: it only ADDS back seconds that were wrongly excluded, so no group day total can shrink and no new retraction can be produced. 🔴 0132 ALSO REPAIRS A SIDE EFFECT I CAUSED THIS MORNING. 0129's apply block calls _repair_orphan_goal_completions (0129:349) and that function's group branch used the today's-members predicate, deleting a group day whenever every contributor had since left. Blast radius was MEASURED, not guessed: the delete criterion was TOTAL ZERO seconds, not below-goal, so only groups where every contributor left are affected. The repair mirrors the delete exactly - three conditions together: no event exists now, the OLD predicate yields zero, and the CORRECTED predicate meets the goal. 🔴 HONEST LIMIT, written into the migration: the third condition uses the group's CURRENT daily_goal_minutes because the historical goal is stored nowhere. A group that LOWERED its goal since could get back a day it did not earn. The alternative - restoring nothing - would leave genuinely earned days deleted. The smaller of two wrongs was chosen deliberately. backfill_goal_completions() was NOT called: it re-judges all history by today's goal and would invent unearned days for anyone who lowered theirs. EVIDENCE AND ITS LIMIT: pgTAP did NOT run on the dev host (Docker engine will not start). supabase/tests/057, 058 and 059 were WRITTEN but never executed here - recorded as OLCEMEDIM, not green. The CI local replay inside this run is their FIRST execution, exactly as it was for 055/056 earlier today, where that same honesty paid off. BOTH ENVIRONMENTS IN ONE SESSION: same standing instruction the owner gave for 0129+0130 on 2026-08-10 - apply staging and production together, do not wait between them. Staging still goes FIRST and its post-check is read before production is dispatched. Once the post-check reports head 0132 this flag is re-locked to false in a separate commit. release_enabled stays false ON PURPOSE. PREVIOUS SCOPE (spent, SUCCEEDED): RE-LOCKED on 2026-08-10 after the 0129 + 0130 apply SUCCEEDED in BOTH environments. EVIDENCE: staging Database Gates run 31380186208 (post-check reported 0129 and 0130 in all three columns on rskiuyjabyzelqododpa, purge health configured / 0 queued / 0 stuck, no SQLSTATE); production run 31380542690 (pre-check showed the two rows missing on the remote side, post-check reported 0129 and 0130 in all three columns on jiphfrpzvkpzubbkhrwb). 🔴 The most useful line of this round: pgTAP could not run on the dev host (Docker engine will not start), so supabase/tests/055 and 056 were delivered as OLCEMEDIM - the CI local replay inside these two runs was the FIRST execution of both files, and both passed on real Postgres. The gate that could not run locally is exactly the gate that mattered. Scope spent; both flags close. migration_head stays 0130 because that is the real state of both environments. PREVIOUS SCOPE (spent, SUCCEEDED): OPEN for exactly ONE production apply of migrations 0129 and 0130. Scope: 0129 + 0130 only. WHAT 0129 DOES (WP-654): deleting a study session did not give the daily streak back. Root cause read from SQL, not from a card: of the four projection triggers on study_sessions, three listen to delete (0050:202, 0063:214, 0063:504) and one does not - 0120:249 is 'after insert or update'. Its body only calls _record_goal_completion (0120:219,239), so the path was MONOTONIC APPEND. 0126 does listen to delete but the table the streak is derived from (goal_progress_events, 0112:204) is never named in 0126 or 0128. 0129 adds the symmetric retraction for both DELETE and UPDATE (shortening a session hit the same gap), skips accounts being deleted with the _account_still_exists predicate 0124 introduced, and carries a DELIBERATELY NARROW backfill: only events with zero remaining seconds behind them are dropped, so a user who later RAISED their daily goal does not lose past streak days they genuinely earned. WHAT 0130 DOES (WP-656): nine FAQ sentences described the product incorrectly. The heaviest was 'signing in again cancels your deletion request' - false, and dangerous: cancelAccountDeletion is only reachable from account_settings_screen.dart:443,596 and has ZERO call sites in the auth flow, so a user trusting that sentence LOSES THE ACCOUNT. Second heaviest: the crown answer cited crownTierFor (gamification.dart:124-128) whose output field is read by no screen at all - the FAQ was citing DEAD CODE as its evidence. 0130 corrects nine entries and adds six missing ones (pricing/ads, XP reversal on delete, the countdown card, the Windows build, no-coercion policy, starting from widget/notification). It carries its own apply guard: a do-block that raises faq_correction_target_missing if any of the 18 target rows is absent, so a silent zero-row UPDATE cannot pass as success. EVIDENCE AND ITS LIMIT - STATED PLAINLY: pgTAP did NOT run locally. Docker Desktop's engine does not start on this host (measured: docker info fails on the npipe endpoint; psql and supabase CLI are absent), so supabase/tests/055 (22 assertions) and 056 were WRITTEN but never executed. This is recorded as OLCEMEDIM, not as green. What WAS measured: the trigger inventory above was read from the migration sources; the FAQ corrections were verified against app code file:line; a Python harness rebuilt the FAQ corpus from 0091+0118+0123, applied 0130 and ran every assertion of 056 over it (66 rows -> 78, TR 39 / EN 39, 21 passed, 0 red), then a sabotage that restored one falsified sentence turned it red. That harness does NOT validate SQL syntax or the do-block guard; the CI database-gates local replay is the first place those run. BOTH ENVIRONMENTS IN ONE SESSION: the owner asked on 2026-08-10 to apply staging and production together rather than wait between them, and was told first that skipping the staging soak saves about three minutes and that 0126 shipped a regression the previous night for exactly that class of reason (repaired by 0128). The owner reaffirmed. Staging is still applied FIRST and its post-check read before production is dispatched - the sequence is kept, only the waiting is dropped. Once the post-check reports head 0130 this flag is re-locked to false in a separate commit. release_enabled stays false ON PURPOSE. PREVIOUS SCOPE (spent, SUCCEEDED): RE-LOCKED on 2026-08-10 after the 0128 production apply SUCCEEDED (run 31336723743). Post-check reported 0128 in all three columns with no SQLSTATE. The repair logged 'WP-635 onarim: 3 kilit satiri geri kondu' and '8 kullanici yeniden uzlastirildi' - three unlock rows that 0126 had wrongly deleted were rebuilt by deriving the highest tier from the surviving ledger, and the frozen best-ever values were pulled down to the real sessions. The migration's own verification passed: no achievement with a ledger tier is missing from the unlock table. All three environments are at 0128. Scope spent. release_enabled stays false ON PURPOSE. PREVIOUS SCOPE (spent, SUCCEEDED): OPEN for exactly ONE production apply of migration 0128 (WP-635). Scope: 0128 only. 🔴 THIS REPAIRS A REGRESSION I SHIPPED TO PRODUCTION EARLIER TONIGHT. 0126's reconciliation matched the single user_achievements row by 'tier = <lost tier>' and DELETED it. That table holds one row per achievement (0022:26) whose tier column is the HIGHEST reached tier, so an account sitting at steel_will tier 6 lost the entire row when tiers 5 and 6 became unearned - taking the display of tiers 1-4 that were still legitimately earned with it. MEASURED ON THE OWNER'S OWN ACCOUNT: xp_ledger holds steel_will t1..t4, day_hero t1..t5 and marathon_total t1 while the screen showed zero tiers. The data survived; the row the UI reads did not. WHAT IT DOES: (1) the unlock row is LOWERED to the highest still-earned tier instead of deleted, and removed only when no tier remains; (2) deleted rows are rebuilt by DERIVING the highest tier from the surviving ledger - not guesswork; (3) achievement_metric_progress is pulled down for the three session-derived metrics, which is why the screen still read 'Best 480' after the owner deleted an 8-hour test session - that projection is cumulative-greatest by design (0050:296) and never decreased. Out-of-scope achievements and metrics are untouched. SELF-VERIFYING: no achievement that still has a ledger tier may be missing from the unlock table afterwards, or the migration raises and the transaction rolls back. EVIDENCE: applied to STAGING on 2026-08-10, post-check 0128 on both sides (run 31335734360). Database Gates green on 48e1c53 with supabase/tests/054 (6 assertions) covering the exact gap tests/052 could not see - 052's 'preserved' assertion read the LEDGER, not the row the screen reads, which is why it stayed green through a real user-visible regression. BACKUP: free plan, no PITR; owner waiver on record since 2026-07-27. AUTHORIZATION: the owner reported the broken screen and asked for it to be fixed. Once the post-check reports head 0128 this flag is re-locked in a separate commit. PREVIOUS SCOPE (spent, SUCCEEDED): RE-LOCKED on 2026-08-09 after the 0126+0127 production apply SUCCEEDED. EVIDENCE: Database Gates run 31334051017; both migrations applied and the post-check reported 0127 in all three columns on jiphfrpzvkpzubbkhrwb with no SQLSTATE. The backfill logged 'WP-634 backfill: 8 kullanici uzlastirildi' - that is the number of profiles PROCESSED, not the number found wrong; the loop visits every profile and the reconciliation is a no-op for anyone whose ledger already matched their sessions. The migration's own verification passed, so every profile's balance now equals its own ledger sum. 🔴 The same run repeated the night's lesson in one line: the LOCAL replay logged '0 kullanici uzlastirildi' while the REMOTE apply logged 8. A gate that only runs on a fresh database cannot see this class of work at all. WHAT THIS CLOSED: deleting a study session now reverses the XP, achievement tiers and crown it produced, both going forward (trigger) and for history (backfill). All three environments are at 0127. Scope spent. release_enabled stays false ON PURPOSE. The free-plan no-backup/no-PITR waiver stays on record. PREVIOUS SCOPE (spent, SUCCEEDED): OPEN for exactly ONE production apply covering migrations 0126 and 0127 (WP-634). Production is at 0125, so this single push applies both. WHY: deleting a study session did not reverse the XP, achievement tiers or crown it produced. A user's timer ran all night, wrote an ~11 hour phantom session; deleting it left everything in place, including a crown rank the user had not earned. Diagnosis: docs/analiz/WP-595-sayac-xp-teshis.md. The owner asked for this explicitly on 2026-08-09 and asked that it be automatic from then on. WHAT 0126 DOES: installs a statement-level AFTER DELETE trigger on study_sessions that RECOMPUTES the user's gamification from their real sessions - it does not subtract, because nothing records how much was added. Scope is deliberately narrow: hour XP, marathon_total, steel_will, day_hero. Achievements awarded by other paths and cumulative-greatest metrics are untouched; treating their progress as zero would have destroyed genuinely earned badges. WHAT 0127 DOES: runs that reconciliation once over existing users, because a trigger cannot fire for a session deleted before it existed. It names no user - the repository is public and a targeted migration would miss anyone else in the same state - and it verifies itself, raising and rolling back if any profile's balance still differs from its own ledger sum. ACCOUNT-DELETION HAZARD: an AFTER DELETE trigger on study_sessions is the exact class that made account deletion impossible in this repo (0124/WP-549, fixed hours earlier the same night). The trigger is silenced by _account_still_exists and supabase/tests/052 asserts 'delete from auth.users' still succeeds with it installed. EVIDENCE: applied to STAGING on 2026-08-09 with the post-check reporting 0127 on both sides (run 31333835770). 🔴 That run also proved the local gate is blind here: the local replay logged '0 kullanici uzlastirildi' (empty database) while the remote staging apply logged '1 kullanici uzlastirildi'. Database Gates green on aa20aaf with tests 052 (12 assertions, including the reverse ones) and 053 (which corrupts a balance on purpose to prove the backfill's self-check is not an empty sentence). BACKUP: free plan, no PITR; owner waiver on record since 2026-07-27. 🔴 IRREVERSIBLE: rows dropped by the reconciliation were never earned, and there is no rollback. This is accepted deliberately - the alternative is leaving a user with a crown they did not earn. Once the post-check reports head 0127 this flag is re-locked to false in a separate commit, together with the guard.tests.ps1 head assertions. release_enabled stays false ON PURPOSE. PREVIOUS SCOPE (spent, SUCCEEDED): RE-LOCKED on 2026-08-09 after the 0125 production apply SUCCEEDED. EVIDENCE: Database Gates run 31326411420, operation production-apply on commit e3c0f4ad3e7b114b56833ace979210c8422cfb0a; the migration list post-check reports 0125 in all three columns on jiphfrpzvkpzubbkhrwb with no SQLSTATE. migration_head stays 0125 because that is now the real state of production; only the gate closes. The scope was one apply and it is spent. All three environments are now at 0125, so the release line is no longer blocked by a head mismatch - but release_enabled stays false ON PURPOSE: cutting a version still requires the owner's explicit word and a single-use PRODUCTION RELEASE GO confirmation string. No further apply or release is authorized without a new scoped owner authorization. The free-plan no-backup/no-PITR/no-rollback waiver stays on record. PREVIOUS SCOPE (spent, SUCCEEDED): OPEN for exactly ONE production apply of migration 0125 (WP-630: monthly report consent default and the cron's target month). Scope: 0125 only. WHAT IT CHANGES: (a) profiles.monthly_report_opt_in default true -> false plus a one-shot backfill of rows that are not already false; (b) reschedules the monthly-report-collector cron with the SAME expression, dropping only the request body so collect-reports computes the previous month itself. BLAST RADIUS: one boolean column on public.profiles and one pg_cron job. The UPDATE touches only monthly_report_opt_in; both triggers on profiles are column-scoped (profiles_public_name_filter on display_name, profiles_title_earned_guard on title_achievement_id) so neither fires - checked, not assumed, per the 0124 lesson. There is no immutability guard on profiles, so the 42501 class that broke 0124 cannot occur here. The same cron block already ran successfully in this environment as part of 0035. USER-VISIBLE EFFECT: none. No monthly report email has ever been sent (send-report has no caller and is deployed nowhere, measured in WP-626), so no row carrying true represents a deliberate choice, and the UI has said 'not being sent yet' since WP-626. EVIDENCE: applied to STAGING on 2026-08-09 with the post-check reporting 0125 on both sides (Database Gates run 31325827247). Clean local replay on real Postgres with pgTAP is green, including the new supabase/tests/051 which builds a real row by hand and asserts the backfill statement actually closes it - a fresh replay leaves profiles empty, so without that the statement would be unmeasured, which is the exact gap that let 0124 reach production. Full CI is green on this commit including both Android emulator smoke runs, Windows golden baselines and Windows integration (run 31325823506); every earlier CI run tonight had been cancelled by the next push, so this is the first complete verification of this code. WHY NOW: adding 0125 re-locked the release line (release-preflight.ps1 requires local head == contract head), so no Windows or Play release can be cut until this is applied. BACKUP: free plan, no PITR and no snapshot; the owner accepted applying without a backup on 2026-07-27 and has reaffirmed it. The workflow resolves real backup evidence from Supabase itself; no evidence string is hand-written. AUTHORIZATION: the owner delegated migration applies in-session on 2026-08-09 conditional on certainty. Once the post-check reports head 0125 this flag is re-locked to false in a separate commit, together with the guard.tests.ps1 head assertions. release_enabled stays false ON PURPOSE - cutting a version still requires the owner's explicit word. PREVIOUS SCOPE (spent, SUCCEEDED): RE-LOCKED on 2026-08-09 after the 0124 production apply SUCCEEDED on the second attempt. EVIDENCE: Database Gates run 31324086033, operation production-apply on commit 34cfd0952530513a420bb47f67bc53dcd23e82f4; the migration list post-check reports 0124 in all three columns (local / remote / applied) on jiphfrpzvkpzubbkhrwb. 'Applying migration 0124_account_purge_indirect_restrict_chains.sql' completed with no SQLSTATE. WHAT THIS CLOSED: account deletion was factually impossible in production for any user who had ever run the timer, had a push record, owned a group with a report, or had been sanctioned - while docs/legal/ACCOUNT-DELETION.* promises it unconditionally and Google Play requires it. THE FIRST ATTEMPT FAILED (run 31323239616, statement 27, moderation_audit_append_only 42501) because the migration's own backfill ran before it replaced the unconditional guard from 0106; staging had passed only because zero rows matched there. Fixed in 15779c9 by suspending immutability for that single statement, with supabase/tests/050 extended to build the pre-migration row by hand and assert BOTH that the bare UPDATE is still rejected and that the wrapped form completes - the old test could not measure the backfill at all, which is exactly how the defect reached production. migration_head stays 0124 because that is now the real state of production; only the gate closes. The scope was one apply and it is spent. No further apply or release is authorized without a new scoped owner authorization. release_enabled stays false ON PURPOSE - this migration needs no new app release. The free-plan no-backup/no-PITR/no-rollback waiver stays on record (production.backup_waiver, owner 2026-07-27). PREVIOUS SCOPE (spent, SUCCEEDED): OPEN for exactly ONE production apply of migration 0124 (WP-549: account deletion is blocked by four indirect restrict FK chains, five write-back triggers and two immutability guards). Scope: 0124 only. This is the SECOND attempt; the first FAILED and the reason is now understood and fixed. WHAT FAILED: run 31323239616 died at statement 27 - the migration's own backfill 'update public.moderation_audit_events set actor_hash = pseudonymous_user_hash(actor_id)' ran BEFORE the migration replaced the unconditional append-only guard from 0106, so the guard rejected it with 42501. Staging passed only because no row matched there, so the UPDATE touched zero rows and the guard never fired. THE FIX (commit 15779c9): immutability is suspended explicitly for that single statement via 'alter table ... disable trigger moderation_audit_events_immutable' and re-enabled immediately after. Re-ordering would NOT have been enough: the new guard only permits 'actor_id non-null -> null with hash unchanged', which is the opposite of what the backfill does. WHY THIS RETRY IS SAFE - three independent lines of evidence. (1) Statements 1 to 26 already executed against REAL production data during the failed attempt before the transaction rolled back, so every data-dependent statement before the failure point is proven on production data, including the 'validate constraint ugc_reports_context_group_only_for_message'. (2) Statement 27 is fixed and the fix is measured with actual rows: supabase/tests/050 now builds a pre-0124 row by hand and asserts BOTH that the bare UPDATE is still rejected (the guard was not weakened) and that the disable/enable form completes the backfill. The old test could not measure this at all - on a fresh replay the table is empty and the backfill touches zero rows, which is exactly how the defect reached production. (3) Everything after statement 27 is pure DDL (function and trigger definitions) and cannot depend on data. Database Gates run 31323807502 is GREEN on the fix, with test 050 running all 29 assertions. WHY NOW: the defect is LIVE in production - any user who has ever run the timer, has a push record, owns a group with a report, or was sanctioned CANNOT delete their account, while docs/legal/ACCOUNT-DELETION.* promises deletion unconditionally and Google Play requires it. BACKUP: free plan, no PITR and no snapshot; the owner accepted applying without a backup on 2026-07-27 and has reaffirmed it. The workflow resolves real backup evidence from Supabase itself via tooling/supabase/backup-evidence.ps1; no evidence string is hand-written. AUTHORIZATION: the owner delegated this apply in-session on 2026-08-09 ('eminsen uygula sana biraktim ama emin ol') and reaffirmed it after the failure ('emin oldugun surece onayim var'), conditional on certainty - which the three evidence lines above establish. Once the post-check reports head 0124 this flag is re-locked to false in a separate commit, together with the guard.tests.ps1 head assertions. release_enabled stays false ON PURPOSE - this migration needs no new app release. PREVIOUS SCOPE (spent, FAILED): OPEN for exactly ONE production apply of migration 0124 (WP-549: account deletion is blocked by four indirect restrict FK chains, five write-back triggers and two immutability guards). Scope: 0124 only. WHY NOW: the defect is LIVE in production - any user who has ever run the timer, has a push record, owns a group with a report, or was sanctioned CANNOT delete their account, while docs/legal/ACCOUNT-DELETION.* promises deletion unconditionally and Google Play requires it. EVIDENCE: the identical migration was applied to STAGING on 2026-08-09 and the post-check reported head 0124 on both sides with the purge health row reading configured / 0 queued / 0 stuck (Database Gates run 31277610025). CI local replay on real Postgres with pgTAP is GREEN (run 31277161339), including supabase/tests/050 (26 assertions). Two earlier replay attempts were RED and both were real defects, not flakes. BACKUP: this project is on the Supabase free plan - there is no PITR and no snapshot. The owner accepted applying without a backup on 2026-07-27 and reaffirmed it. The workflow resolves the real backup evidence from Supabase itself via tooling/supabase/backup-evidence.ps1; no evidence string is hand-written. AUTHORIZATION: the owner granted this apply in-session on 2026-08-08, reaffirmed it on 2026-08-09 and explicitly permitted this gate edit. The earlier attempt to open this gate was blocked by the automatic safety classifier and was NOT forced. Once the post-check reports head 0124 this flag is re-locked to false in a separate commit, together with the guard.tests.ps1 head assertions. release_enabled stays false ON PURPOSE - this migration needs no new app release. PREVIOUS SCOPE (spent): RE-LOCKED after the 2026-08-08 WP-522 apply: migration 0123 (FAQ content expansion) was applied to production and the post-check reported head 0123 (run 31258274401). ORIGINAL OPEN SCOPE was exactly ONE production apply of migration 0123 (WP-522 FAQ content expansion, 40 rows, TR+EN) after the staging apply post-checks head 0123. Content-only: no schema, function, policy or grant changes; blast radius is the faq_entries table alone and the rollback is a plain delete (written in the migration header). Authorized by the owner in-session on 2026-08-08. Once the post-check reports head 0123 this flag is re-locked to false in a separate commit, together with the guard.tests.ps1 head assertions. release_enabled stays false ON PURPOSE - FAQ rows are read from the server, so no new APK is required. That scope is now spent; no further apply or release is authorized without a new scoped owner authorization. The free-plan no-backup/no-PITR/no-rollback waiver stays on record (production.backup_waiver, owner 2026-07-27). Both heads now read 0122 in all four pin locations. release_enabled stays false ON PURPOSE - the stable release passes with a single-use PRODUCTION RELEASE GO confirmation string instead of an open flag. No further apply or release is authorized; a new scoped owner authorization is required. The free-plan no-backup/no-PITR/no-rollback waiver stays on record (production.backup_waiver, owner 2026-07-27)."
  }
}
