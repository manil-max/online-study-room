<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!--
      WP-110: Play Store kanalı.
      REQUEST_INSTALL_PACKAGES buraya EKLENMEZ (main'de de yok).
      applicationId = com.manilmax.online_study_room (stable ile aynı; ikinci ürün yok).
      WP-128: distributionChannel meta-data denetim/grep için; Dart otoritesi
      FLUTTER_APP_FLAVOR + DISTRIBUTION_CHANNEL (DistributionConfig).
    -->

    <!--
      WP-537: `open_filex` eklentisi DÖRT geniş medya iznini kendi manifestinde
      bildiriyor ve manifest birleştirme bunları uygulamaya taşıyor. Ölçüldü:
      `playRelease` birleştirilmiş manifestinde READ_MEDIA_IMAGES,
      READ_MEDIA_VIDEO, READ_MEDIA_AUDIO ve READ_EXTERNAL_STORAGE vardı —
      hiçbiri bizim manifestimizde yazılı değil.

      🔴 Bu izinlerin karşılığı olan tek kod yolu `updater_dialog.dart`
      (indirilen APK'yı açmak) ve o yol Play sürümünde ZATEN KAPALI
      (`DistributionConfig.allowsSideloadUpdates == false`). Yani Play sürümü,
      hiç çalışmayacak bir özellik için kullanıcının müzik ve video
      kütüphanesine erişim istiyordu.

      Play tarafında bedeli somut: READ_MEDIA_IMAGES/VIDEO "Photo and Video
      Permissions" beyanını, READ_MEDIA_AUDIO ise "Music and audio files"
      kısıtlı izin beyanını tetikler. Bir çalışma uygulamasının müzik
      kütüphanesine erişme gerekçesi yok; ilk gönderimde en olası soru budur.

      Avatar/ekran görüntüsü seçme bundan ETKİLENMEZ: `image_picker_android`
      bu izinleri bildirmiyor, sistem foto seçicisini kullanıyor.

      Kalıcı kapı: test_all.py icindeki `play-manifest` kapisi.
    -->
    <uses-permission
        android:name="android.permission.READ_MEDIA_IMAGES"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.READ_MEDIA_VIDEO"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.READ_MEDIA_AUDIO"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        tools:node="remove" />

    <!--
      WP-544: `USE_EXACT_ALARM` Play kanalindan DUSURULUR.

      Play Console "Tam alarmlar" beyani yalnizca iki secenek sunuyor: uygulaman
      "Calar saat" mi yoksa "Takvim" mi? Odak Kampi ikisi de degil (magaza
      tanimi: calisma/odak uygulamasi). Formun kendi metni: temel islevin bunlar
      degilse "izni tum kanallarda uygulamanizdan kaldirmaniz gerekir".
      Olculdu: v61 gonderiminde bu hata sürüm yayinlamayi bloke etti.

      Kaldirilan yalniz `USE_EXACT_ALARM` (on-verilen izin). `SCHEDULE_EXACT_ALARM`
      KALIR ve kullanicidan istenir: `ExactAlarmHelper.kt` +
      `lib/core/time_engine/exact_alarm_permission.dart`; istek noktasi
      `features/clock/alarms_screen.dart`. Izin verilmezse
      `ExactAlarmPermission.scheduleMode()` sessizce `inexactAllowWhileIdle`a
      duser, yani alarm yine calar, saniyesi garanti olmaz.

      GitHub kanallari (stable/beta) ETKILENMEZ: bu dosya yalniz play flavor'ina
      uygulanir, oradaki alarm davranisi aynen kalir.
    -->
    <uses-permission
        android:name="android.permission.USE_EXACT_ALARM"
        tools:node="remove" />

    <!--
      WP-579: `passkeys_android` UC izin sizdiriyor ve ucunun de arkasinda
      OZELLIK YOK.

      Paket dogrudan bagimlilik degil: `supabase_flutter -> passkeys`
      transitifi (`pubspec.lock` icinde ucu de `dependency: transitive`).
      Olculdu, tahmin degil:
        app/lib icinde passkey/WebAuthn/CredentialManager/OAuth cagrisi  -> 0
        app/android/app/src icinde (kt/xml) ayni arama                   -> 0
      Yani uygulama bu akisi HIC cagirmiyor; izinler yalniz manifest
      birlestirmesiyle geliyor.

      Play magaza sayfasi izin listesini kullaniciya gosterir. Bir calisma
      uygulamasinin karsiligi olan tek bir ekran bile yokken "biyometrik" ve
      "kimlik bilgileri" izni istemesi incelemede sorulacak ilk seylerden
      biridir ve kullanicida guvensizlik uretir. WP-537 (medya izinleri) ve
      WP-544 (USE_EXACT_ALARM) ayni gerekceyle ayni yolu izledi.

      Ileride gercekten passkey ile giris eklenirse: bu blok kaldirilir ve
      izinler beyaz listeye (test_all.py `PLAY_ALLOWED_PERMISSIONS`) gerekcesiyle
      geri yazilir. Beyaz liste kapisi zaten ikisini birden zorunlu kilar.

      GitHub kanallari (stable/beta) ETKILENMEZ: bu dosya yalniz play flavor'ina
      uygulanir.
    -->
    <uses-permission
        android:name="android.permission.USE_BIOMETRIC"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.USE_CREDENTIALS"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.CREDENTIAL_MANAGER_SET_ORIGIN"
        tools:node="remove" />

    <application>
        <meta-data
            android:name="com.manilmax.online_study_room.DISTRIBUTION_CHANNEL"
            android:value="${distributionChannel}" />
    </application>
</manifest>
