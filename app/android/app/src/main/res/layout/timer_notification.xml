<?xml version="1.0" encoding="utf-8"?>
<!-- WP-205: Yalnız akan HH:MM:SS. Başlat/Durdur/Mola artık NATIVE aksiyon
     butonları (sistem stili → her zaman görünür). Eski pill (#26FFFFFF) + beyaz
     kronometre açık bildirim temasında görünmez oluyordu; renk sistem bildirim
     TextAppearance'ından gelir (light/dark uyumlu). -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingStart="4dp"
    android:paddingTop="4dp"
    android:paddingEnd="8dp"
    android:paddingBottom="4dp">

    <Chronometer
        android:id="@+id/notif_timer_elapsed"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fontFamily="sans-serif-medium"
        android:gravity="start|center_vertical"
        android:maxLines="1"
        android:text="00:00:00"
        android:textAppearance="@style/TextAppearance.Compat.Notification.Title"
        android:textSize="28sp"
        android:textStyle="bold" />
</LinearLayout>
