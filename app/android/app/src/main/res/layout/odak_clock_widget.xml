<?xml version="1.0" encoding="utf-8"?>
<!-- 2x1'de yalniz saat; yukseklik arttikca tarih eklenir. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/clock_widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_card_bg"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="4dp">

    <TextClock
        android:id="@+id/clock_widget_time"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:format12Hour="h:mm"
        android:format24Hour="HH:mm"
        android:gravity="center"
        android:maxLines="1"
        android:textColor="@color/widget_design_ink"
        android:textSize="18sp"
        android:textStyle="bold" />

    <TextClock
        android:id="@+id/clock_widget_date"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:ellipsize="end"
        android:format12Hour="EEE d MMM"
        android:format24Hour="EEE d MMM"
        android:gravity="center"
        android:maxLines="1"
        android:textColor="@color/widget_design_ink_muted"
        android:textSize="11sp"
        android:visibility="gone" />
</LinearLayout>
